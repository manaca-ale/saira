# A_v3 base prompt

Origem: `services/yolo-worker-vm/src/worker/_prompts_v3.py::NEW_LITTER_SYSTEM_PROMPT_V3`

Snapshot completo não foi duplicado aqui para evitar divergência manual. A campanha é reproduzível pelo `env-snapshot.yaml`:

- `git.commit`: `ea6dc3febc4c9460b1d54c97c734fc461086f45b`
- `git.branch`: `develop`
- `git.dirty`: `true`

Como `git.dirty` é `true`, a referência exata do prompt base deve ser lida do workspace desta campanha/repo, não apenas do SHA.
