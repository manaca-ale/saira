# Keep the build context limited to what the Dockerfile actually copies.
# If new COPY inputs are added to the Dockerfile, update this allowlist too.
**
!Dockerfile
!requirements.txt
!server.py
!templates/
!templates/**
!static/
!static/**
