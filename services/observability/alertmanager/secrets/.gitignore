# Keep this directory in git so the compose bind-mount has a real path, but never
# commit the actual secret. Place a file named `resend_api_key` here on the server
# (contents = the Resend API key, no trailing newline needed).
*
!.gitignore
